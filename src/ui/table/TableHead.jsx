import styled from "styled-components";

export const TableHead = styled.p`
  color: var(--color-primary);
  font-weight: 700;
  text-align: start;
  font-size: 16px;
  text-wrap: balance;
  line-height: 20px;
`;
